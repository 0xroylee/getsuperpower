interface:
  display_name: "Ponyrace"
  short_description: "Run requirement discussion from chat"
  default_prompt: "Use $ponyrace to run a worker-backed Ponyrace requirement discussion before implementation; add --no-research only when the user explicitly wants local deterministic pony review."
