# Tasks

## 1. Proposal And Design

- [x] Confirm the folder should be removed because Omniskill is now the default project.
- [x] Record the target source location: `src/omniskill.ts`.

## 2. Implement With TDD

- [x] Update tests to expect the command module in `src/`.
- [x] Move command implementation into `src/omniskill.ts`.
- [x] Update imports in `src/cli.ts`.
- [x] Delete `omniskill-command/`.
- [x] Update docs and AGENTS references.

## 3. Verify

- [x] Run focused Omniskill and CLI tests.
- [x] Run `rtk bun run check`.
- [x] Smoke test root and compatibility command paths.
- [x] Record Pony Trail post-change evidence.
- [ ] Run `/opsx:archive` after human approval.
